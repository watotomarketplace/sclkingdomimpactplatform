{
  "buildCommand": "npx prisma migrate deploy && npx prisma generate && npx tsx prisma/seed.ts && next build",
  "framework": "nextjs",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/milestone-sla",
      "schedule": "0 8 * * *"
    }
  ]
}
