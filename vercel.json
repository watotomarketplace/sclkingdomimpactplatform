{
  "buildCommand": "npx prisma migrate deploy && next build",
  "framework": "nextjs",
  "installCommand": "npm install",
  "integrations": [
    {
      "slug": "neon",
      "requiredFor": "build"
    }
  ]
}
